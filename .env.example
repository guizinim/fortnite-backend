# Firebase Service Account JSON (complete JSON as single line or multiline)
# Get this from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project-id",...}

# Alternative: Path to service account JSON file (local development only)
# FIREBASE_SERVICE_ACCOUNT_PATH=./banco-users-firebase-adminsdk-fbsvc-7ef36fcf4c.json

# Node environment
NODE_ENV=development

# Port for local server (Vercel will override this in production)
PORT=4000
